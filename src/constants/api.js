// URL du backend Versions (decode-api).
// Le projet Vercel "decode" est lié au repo GoldeyMusic/decode-api.
// L'ancien backend Railway (decode-api-production.up.railway.app) est
// abandonné depuis qu'on est passé full Vercel.
const API = "https://decode-kappa.vercel.app";

export default API;
